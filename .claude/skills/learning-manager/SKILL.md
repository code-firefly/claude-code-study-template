---
name: learning-manager
description: AI 技术学习模块管理 - 开始学习新模块、完成模块学习、重置模块进度。当用户说"开始学习"、"我要学..."、"进入...模块"、"学完了"、"搞定"、"完成学习"、"重置模块"、"重新开始"时触发此技能。
---

# Learning Manager Skill

## 功能范围
- 开始学习新模块（选择模式、初始化状态）
- 完成模块学习（验证完成度）
- 重置模块进度
- 检查知识缓存状态

## 模块路径映射

### 新名称（v2.0）
- `ai-tools-fundamentals` → `01-基础入门/ai-tools-fundamentals`
- `mcp-protocol` → `01-基础入门/mcp-protocol`
- `agent-configuration` → `02-进阶探索/agent-configuration`
- `mcp-advanced-config` → `02-进阶探索/mcp-advanced-config`
- `ai-orchestration` → `02-进阶探索/ai-orchestration`
- `ai-resources-research` → `02-进阶探索/ai-resources-research`
- `config-management` → `03-实战应用/config-management`
- `spec-driven-dev` → `03-实战应用/spec-driven-dev`
- `practical-projects` → `03-实战应用/practical-projects`

### 旧名称（v1.x 向后兼容，含别名映射）
- `claude-code-core` → `01-基础入门/claude-code-core`（别名：ai-tools-fundamentals）
- `mcp-basics` → `01-基础入门/mcp-basics`（别名：mcp-protocol）
- `agent-sdk` → `02-进阶探索/agent-sdk`（别名：agent-configuration）
- `mcp-advanced` → `02-进阶探索/mcp-advanced`（别名：mcp-advanced-config）
- `openclaw-ecosystem` → `02-进阶探索/openclaw-ecosystem`（别名：ai-orchestration）
- `everything-claude-code` → `02-进阶探索/everything-claude-code`（别名：ai-resources-research）
- `cc-switch` → `03-实战应用/cc-switch`（别名：config-management）
- `spec-kit` → `03-实战应用/spec-kit`（别名：spec-driven-dev）
- `projects` → `03-实战应用/projects`（别名：practical-projects）

---

## 开始学习

**触发方式**（任意一种）：
- "开始学习 `<模块名>`" / "启动 `<模块名>` 的学习" / "开始学习模块 `<模块名>`"
- "我要学 `<模块名>`" / "我要开始学 `<模块名>`" / "我想学 `<模块名>`"
- "开始 `<模块名>`" / "进入 `<模块名>` 模块" / "学习 `<模块名>`"
- "学 `<模块名>`" / "启动 `<模块名>`" / "开启 `<模块名>` 学习"
- "`<模块名>` 开始学习" / "对 `<模块名>` 开始学习"

**执行步骤**：

### 步骤 0：非阻塞更新检查
1. 从 CHANGELOG.md 提取本地版本（查找 `## [版本号]` 模式）
2. 尝试从 upstream 获取上游版本（如配置了 upstream）
3. 如上游版本更新，存储更新提醒字符串（格式如下）：
   ```
   ╔════════════════════════════════════════════════════════════════╗
   ║  📢 发现新版本                                                 ║
   ║                                                                ║
   ║  当前版本: v2.0.1  |  上游版本: v2.1.0                        ║
   ║                                                                ║
   ║  建议：学习结束后运行 "同步学习计划" 获取最新功能             ║
   ╚════════════════════════════════════════════════════════════════╝

   [以下是命令结果...]
   ```
4. 在最终输出前，如存在更新提醒，则添加到结果开头

### 步骤 0.5：检查知识缓存
1. 读取模块的 `knowledge/.metadata.json`（如果存在）
2. **情况 A：缓存存在**
   - 显示：`发现本地缓存 (缓存日期: YYYY-MM-DD)`
   - 询问用户：
     - 1. 使用本地缓存（快速开始）
     - 2. 更新缓存（拉取最新资料）
3. **情况 B：缓存不存在**
   - 显示：`未找到知识缓存`
   - 询问用户：
     - 1. 初始化缓存（推荐，首次拉取）
     - 2. 跳过缓存（直接开始学习）

### 步骤 1：模块名称别名解析
如果用户输入的模块名不在映射表中，检查别名映射：

| 旧名称 | 新名称 | 说明 |
|--------|--------|------|
| `claude-code-core` | `ai-tools-fundamentals` | AI 工具基础 |
| `mcp-basics` | `mcp-protocol` | MCP 协议入门 |
| `agent-sdk` | `agent-configuration` | Agent 配置与使用 |
| `mcp-advanced` | `mcp-advanced-config` | MCP 高级配置 |
| `openclaw-ecosystem` | `ai-orchestration` | AI 编排平台 |
| `everything-claude-code` | `ai-resources-research` | AI 资源研究 |
| `cc-switch` | `config-management` | 配置管理工具 |
| `spec-kit` | `spec-driven-dev` | 规范驱动开发 |
| `projects` | `practical-projects` | 综合实战项目 |

如果找到别名：
- 显示提示：`"注意：'{旧名称}' 已更名为 '{新名称}'，正在使用新名称继续..."`
- 使用新名称继续后续流程

### 步骤 2：查找模块路径
从映射表查找模块路径，优先匹配新名称。

### 步骤 3：验证模块状态
检查模块是否已开始学习（检查 checklist.md 的学习模式选择）
- 如果已开始，显示警告信息

### 步骤 4：选择学习模式
询问用户选择：
- 1. 📚 快速模式（约 3 天）- 理论为主
- 2. 🛠️ 完整模式（约 1 周）- 理论+实践

### 步骤 5：显示更新预览
- 模块信息（名称、优先级、模式、开始日期）
- 将要更新的文件列表
- 等待用户确认

### 步骤 6：更新文件
用户确认后，更新以下文件：
- **checklist.md**：勾选对应的学习模式 checkbox
- **notes.md**：设置学习模式和开始日期
- **PROGRESS.md**：更新模块状态为"进行中"，添加学习日志

### 步骤 7：显示成功信息
显示成功信息和下一步指导

---

## 完成学习

**触发方式**（任意一种）：
- "完成学习 `<模块名>`" / "标记 `<模块名>` 为已完成" / "完成模块 `<模块名>`"
- "学完了 `<模块名>`" / "`<模块名>` 搞定了" / "`<模块名>` 学完了"
- "结束学习 `<模块名>`" / "`<模块名>` 学习结束" / "`<模块名>` 完成了"
- "`<模块名>` 已经学完" / "搞定 `<模块名>`" / "`<模块名>` 完成"
- "标记完成 `<模块名>`" / "`<模块名>` 已完成" / "完成 `<模块名>` 模块"

**执行步骤**：

### 步骤 0：非阻塞更新检查
1. 从 CHANGELOG.md 提取本地版本（查找 `## [版本号]` 模式）
2. 尝试从 upstream 获取上游版本（如配置了 upstream）
3. 如上游版本更新，存储更新提醒字符串（格式如下）：
   ```
   ╔════════════════════════════════════════════════════════════════╗
   ║  📢 发现新版本                                                 ║
   ║                                                                ║
   ║  当前版本: v2.0.1  |  上游版本: v2.1.0                        ║
   ║                                                                ║
   ║  建议：学习结束后运行 "同步学习计划" 获取最新功能             ║
   ╚════════════════════════════════════════════════════════════════╝

   [以下是命令结果...]
   ```
4. 在最终输出前，如存在更新提醒，则添加到结果开头

### 步骤 1：解析 checklist.md 计算完成百分比

### 步骤 2：验证完成情况：
   - 如果完成度 < 100%，显示警告和未完成项列表
   - 询问是否确认完成
   - 如果用户坚持，可以强制完成（即使有未完成项）

### 步骤 3：显示完成预览：
   - 最终完成百分比
   - 完成日期
   - 将要更新的文件

### 步骤 4：用户确认后，更新以下文件：
   - **notes.md**：记录完成日期
   - **PROGRESS.md**：更新状态为"已完成"，进度设为 100%，添加完成日志

### 步骤 5：重新计算总体进度和各优先级进度

---

## 重置模块

**触发方式**（任意一种）：
- "重置模块 `<模块名>`" / "重新开始 `<模块名>`" / "清除 `<模块名>` 的进度"
- "重学 `<模块名>`" / "`<模块名>` 重新开始" / "从头开始 `<模块名>`"
- "清除进度 `<模块名>`" / "重置 `<模块名>`" / "`<模块名>` 清零"

**执行步骤**：

1. 显示警告：此操作将清除模块的所有学习进度

2. 询问用户确认

3. 重置以下文件：
   - **checklist.md**：取消所有勾选项，清除学习模式选择
   - **notes.md**：重置日期和内容
   - **PROGRESS.md**：状态改为"未开始"，进度改为 0%

4. 添加重置日志

---

## 文件操作模式

### checklist.md 更新模式

```markdown
# 修改前
- [ ] **快速模式**（约 3 天）- 理论为主
- [x] **完整模式**（约 1 周）- 理论+实践

# 修改为（快速模式）
- [x] **快速模式**（约 3 天）- 理论为主
- [ ] **完整模式**（约 1 周）- 理论+实践
```

### notes.md 更新模式

```markdown
# 修改前
> **学习模式**：待选择
> **开始日期**：待定

# 修改为（开始快速模式学习）
> **学习模式**：快速模式
> **开始日期**：2026-03-07
```

### PROGRESS.md 更新模式

```markdown
# 修改模块行
# 从：
| ai-tools-fundamentals | P0 🔴 | 未选择 | 未开始 | 0% |

# 改为（开始快速模式）：
| ai-tools-fundamentals | P0 🔴 | 快速模式 | 进行中 | 0% |

# 添加学习日志
| 2026-03-07 | ai-tools-fundamentals | 开始学习 | 选择快速模式 |
```

---

## 注意事项

1. **用户确认**：修改多个文件前，总是先显示预览并等待用户确认
2. **日期格式**：统一使用 YYYY-MM-DD 格式
3. **文件路径**：所有路径相对于仓库根目录
4. **错误处理**：如果文件不存在或模块未找到，给出明确的错误提示
5. **进度计算**：基于实际勾选的 checkbox 数量，不要估算
